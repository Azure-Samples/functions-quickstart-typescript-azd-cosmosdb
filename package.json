{
  "name": "functions-quickstart-typescript-azd-cosmosdb",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "npm run build && node --test dist/test/cosmos_trigger.test.js"
  },
  "dependencies": {
    "@azure/functions": "4.16.5"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "typescript": "^4.0.0",
    "rimraf": "^5.0.0"
  },
  "main": "dist/src/{index.js,functions/*.js}"
}